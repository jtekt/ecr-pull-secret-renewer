stages:
  - deploy

deploy:
  stage: deploy
  only:
    - master
    - main
  script:
    - kubectl apply -f ./ecr-crednetials-renewer.yml
  environment:
    name: onpremise
    kubernetes:
      namespace: ${K8S_NAMESPACE}
